import { AnimationPage3Directive } from './animation-page3.directive';

describe('AnimationPage3Directive', () => {
  it('should create an instance', () => {
    const directive = new AnimationPage3Directive();
    expect(directive).toBeTruthy();
  });
});

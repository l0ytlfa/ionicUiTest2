import { AnimationPage1Directive } from './animation-page1.directive';

describe('AnimationPage1Directive', () => {
  it('should create an instance', () => {
    const directive = new AnimationPage1Directive();
    expect(directive).toBeTruthy();
  });
});

import { AnimationPage2Directive } from './animation-page2.directive';

describe('AnimationPage2Directive', () => {
  it('should create an instance', () => {
    const directive = new AnimationPage2Directive();
    expect(directive).toBeTruthy();
  });
});
